// import { Mens } from "../componants/Category/Mens"

export const ShopCategory = () => {
  return (
    <>
      {/* <Mens/> */}
      {/* {
        && all_product.map((item, index) => {
          return item.category == "men"  ? <Item key={index} id={item.id} name={item.name} image={item.image} new_price={item.new_price} old_price={item.old_price} /> :
            item.category == "women" ? <Item key={index} id={item.id} name={item.name} image={item.image} new_price={item.new_price} old_price={item.old_price} /> :
              item.category == "kid" ? <Item key={index} id={item.id} name={item.name} image={item.image} new_price={item.new_price} old_price={item.old_price} /> : ""
        })
      } */}
    </>
  )
}
